/* calculates Highest Common Factor or Greatest common Divisor using eulcid's algorithm */

#include<stdio.h>

int <b>gcd(</b>int a,int b){
    while(a>0){
      if(b>a){
        int temp=b;
        b=a;
        a=temp;
      }
      a=a-b;
    }
    return b;
}
